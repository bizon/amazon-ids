name: Release

on: [workflow_dispatch]

concurrency: release

jobs:
  release:
    name: Run semantic release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.BOT_GITHUB_TOKEN_PUBLIC }}

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14
          cache: yarn

      - name: Install dependencies
        run: yarn --frozen-lockfile --non-interactive --prefer-offline

      - name: Run semantic release
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.BOT_GITHUB_TOKEN_PUBLIC }}
          GIT_AUTHOR_NAME: Bizon Bot
          GIT_AUTHOR_EMAIL: dev@bizon.solutions
          GIT_COMMITTER_NAME: Bizon Bot
          GIT_COMMITTER_EMAIL: dev@bizon.solutions
        run: yarn semantic-release
